# ===== Web framework =====
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart>=0.0.27

# ===== DB =====
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# ===== Validation & settings =====
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ===== Auth =====
python-jose[cryptography]>=3.4.0
# passlib (יצא 2020) לא תואם ל-bcrypt 4.x. ב-deploy זה נופל על:
# 1) AttributeError: module 'bcrypt' has no attribute '__about__'
# 2) ValueError ב-detect_wrap_bug עם > 72 bytes.
# הוחלף בשימוש ישיר ב-bcrypt (ראה app/core/security.py).
bcrypt>=4.1.0,<5.0

# ===== HTTP & external APIs =====
httpx==0.28.1
anthropic==0.42.0
# OpenAI — ספק AI שני, בלעדי לתמלול קולי (§13.3, gpt-4o-transcribe).
# נפרד מ-anthropic — שני מפתחות, שני לוגי עלות.
openai>=1.0

# ===== Phase 3 Stage 16: ניקוי תוכן מייל לפני AI =====
# beautifulsoup4 לפרסור HTML. lxml = parser מהיר; fallback ל-html.parser
# אם lxml לא זמין (נדיר ב-Python 3.12, אבל הקוד מטפל גם בלעדיו).
beautifulsoup4==4.12.3
lxml==5.3.0

# ===== Cryptography =====
# Fernet להצפנת tokens של Google ב-DB. כבר transitively של python-jose,
# אבל נדרש מפורש כי app/utils/encryption.py מייבא ישירות.
cryptography>=42.0.0

# ===== Google integrations (פאזה 2) =====
google-api-python-client==2.157.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1

# ===== Telegram =====
python-telegram-bot==21.9

# ===== Misc =====
python-dateutil==2.9.0.post0
pytz==2024.2
holidays==0.62

# ===== Dev/Test =====
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
